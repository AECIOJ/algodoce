"""Definições da estrutura (camada de dados) do framework ajsystem.

`defs/` agrupa as definições puras da estrutura — dataclasses, tabelas de
tipos, constantes e resolução de campos. Não depende de renderização
(`core/form`, `core/list`, `core/report`), de request (`handles`) nem do
motor (`core/auto`, `core/menu`).

Dependências: apenas stdlib + `app.ajsystem.core.utils` (helpers genéricos).
"""
